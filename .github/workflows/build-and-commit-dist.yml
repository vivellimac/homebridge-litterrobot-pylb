name: Build & Commit dist

on:
  push:
    branches:
      - main
      - api-hookup
      - feature/**
      - fix/**
      - hotfix/**
    paths:
      - "src/**"
      - "tsconfig.json"
      - "package.json"
      - ".eslintrc.*"
    paths-ignore:
      - "dist/**"         # prevent loops on the bot's dist commit
  pull_request:
    branches:
      - main
      - api-hookup
    paths:
      - "src/**"
      - "tsconfig.json"
      - "package.json"
      - ".eslintrc.*"
  workflow_dispatch: {}    # manual run when needed

permissions:
  contents: write          # required to push dist updates

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0            # needed to push back to the same branch
          persist-credentials: true # use GITHUB_TOKEN for the commit

      - name: Use Node 20 LTS
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Lint (no warnings)
        run: npm run lint --max-warnings=0

      - name: Type-check & build
        run: npm run build

      - name: Commit dist if changed
        uses: EndBug/add-and-commit@v9
        with:
          add: "dist"
          message: "ci: build dist [skip ci]"
          default_author: github_actions
          push: true
